# Native folder

This folder contains the original native-focused app and supporting components.

- `src/native/app`: the native app screens and layout
- `src/native/components`: the native components used by that app

The new simple React Native project now lives in `src/app`.
